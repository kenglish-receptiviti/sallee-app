// TODO
// Calculate a width value from `sentiment`. SALLEE sentiment can range from -1
// to 1 and the output should range from 0 to `fullWidth`, respectively.
export const getSentimentWidth = (sentiment: number, fullWidth: number): number =>  {
  return 0;
};
