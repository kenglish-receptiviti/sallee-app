// TODO
// Calculate `width` from `props.sallee.sentiment`. SALLEE sentiment can
// range from -1 to 1, so `width` should range from 0 to `2 * halfWidth`.
export const getSentimentWidth = (sentiment: number, fullWidth: number): number =>  {
  return 0;
};
